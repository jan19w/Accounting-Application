# 🚀 快速启动指南

## 第一步：初始化数据库

1. 确保MySQL服务已启动
2. 在MySQL中执行初始化脚本：

```bash
# 方法1：命令行执行
mysql -u root -p < server/init.sql

# 方法2：MySQL Workbench或其他客户端
# 打开 server/init.sql 文件并执行其中的SQL语句
```

3. 验证数据库是否创建成功：

```sql
USE projectdata;
SHOW TABLES;
DESC details;
```

## 第二步：配置数据库连接

如果你的MySQL账号密码已经在 `server/server.js` 中配置好了，可以跳过此步骤。

否则，编辑 `server/server.js` 第14-18行：

```javascript
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',           // 改为你的用户名
    password: 'password',   // 改为你的密码
    database: 'projectdata'
});
```

## 第三步：启动后端服务器

```bash
cd server
npm install      # 首次运行需要安装依赖
npm start        # 启动服务器
```

你应该看到：
```
数据库连接成功！
服务器正在运行在 http://localhost:5000
```

## 第四步：启动前端应用

打开**新的终端窗口**，在项目根目录执行：

```bash
cd d:\myproject\project2\react-bookkeeper
npm install      # 首次运行需要安装依赖
npm start        # 启动React应用
```

浏览器会自动打开 http://localhost:3000

## ✅ 功能测试清单

### 1. 查询功能
- [ ] 页面加载时自动显示数据库中的所有记录
- [ ] 看到示例数据（如果执行了init.sql的INSERT语句）

### 2. 新增功能
- [ ] 点击"记一笔"按钮
- [ ] 填写表单（类型、日期、类别、金额、备注）
- [ ] 点击"添加"按钮
- [ ] 看到"添加成功！"提示
- [ ] 新记录出现在表格中

### 3. 编辑功能
- [ ] 点击任意记录的"编辑"按钮
- [ ] 修改表单内容
- [ ] 点击"保存"按钮
- [ ] 看到"更新成功！"提示
- [ ] 记录在表格中更新

### 4. 删除功能
- [ ] 点击任意记录的"删除"按钮
- [ ] 确认删除
- [ ] 看到"删除成功！"提示
- [ ] 记录从表格中消失

### 5. 筛选功能
- [ ] 使用顶部筛选器（类型、日期、类别、关键词）
- [ ] 表格实时更新显示筛选结果

### 6. 统计功能
- [ ] 查看KPI卡片（总收入、总支出、净收支）
- [ ] 查看饼图（分类占比）
- [ ] 查看柱状图（时间趋势）

### 7. 批量清空
- [ ] 点击"清空全部"按钮
- [ ] 确认操作
- [ ] 看到"所有数据已清空！"提示
- [ ] 表格变空

## 🔧 常见问题

### 1. "加载数据失败"
**原因：** 后端服务器未启动或数据库连接失败

**解决：**
- 检查后端服务器是否在运行（http://localhost:5000）
- 检查MySQL服务是否启动
- 检查数据库配置是否正确

### 2. "操作失败"
**原因：** API请求失败

**解决：**
- 打开浏览器控制台（F12）查看错误信息
- 检查后端服务器日志
- 确认details表存在且结构正确

### 3. CORS错误
**原因：** 跨域问题（不应该出现，因为已配置）

**解决：**
- 确认server.js中已有 `app.use(cors());`
- 重启后端服务器

### 4. 端口被占用
**错误信息：** `Error: listen EADDRINUSE: address already in use :::5000`

**解决：**
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <进程ID> /F

# 或者修改server.js中的PORT变量
```

## 📊 数据库查询命令

在MySQL客户端中可以使用以下命令查看数据：

```sql
-- 查看所有记录
SELECT * FROM projectdata.details ORDER BY date DESC;

-- 查看收入记录
SELECT * FROM projectdata.details WHERE type = 'income';

-- 查看支出记录
SELECT * FROM projectdata.details WHERE type = 'expense';

-- 统计总收入
SELECT SUM(amount) as total_income FROM projectdata.details WHERE type = 'income';

-- 统计总支出
SELECT SUM(amount) as total_expense FROM projectdata.details WHERE type = 'expense';

-- 按类别统计
SELECT category, type, SUM(amount) as total 
FROM projectdata.details 
GROUP BY category, type 
ORDER BY total DESC;
```

## 🎉 完成！

现在你的记账本应用已经完全连接到MySQL数据库了！所有的增删改查操作都会实时同步到数据库中。


